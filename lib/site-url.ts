/** The site's canonical public domain — shared by layout.tsx's metadataBase,
 *  sitemap.ts, and robots.ts, so it's set in exactly one place. */
export const SITE_URL = "https://bhutaneseincanberra.org.au";
